# PLACEHOLDERS

Everything on screen must trace to a `manifest.json` entry whose hash came
from the archive. The ONLY exceptions are listed here, each with what is
missing, why the original cannot be reproduced, and what stands in. The
fidelity judge reviews this file every phase and may reject an entry.

| What | Why it cannot be original | Stand-in |
|---|---|---|
| `file://` image paths (40 controls, all in `dashuisk/skin.xur`) | The console filled these from device storage at runtime (wallpapers, gamer pictures); every one is an empty path in the data. | Nothing is rendered for them today and nothing needs to be: all 40 sit on `XuiScrollEnd` controls whose visuals contain no `XuiImagePresenter`, so no renderer ever consumes the path. `__dash.deviceFiles` counts any that reach a renderer and is 0 on all 263 scenes. If the glue phase wires wallpapers or gamer pictures, they come from the same runtime sources the console used and are listed here first. |
| Code-driven visual states | Some visuals rest on a frame where the console's code had already moved the playhead. | Mostly closed: the shell now drives `metaScene_1line` through `GotoIndex` on every focus change and the scroll-end arrows off the list's own window. What is left is reported in `__dash.codeDrivenStates` (hidden-children ratio per visual) and never painted from a guess. Not a substitution: the data is the original, the playhead is driven by the same rules the executable used. |
| Blade tabs at rest | `dashmain.xur`'s `Tab1..Tab6` scenes carry `Opacity 0`; the console's shell code raised the active blade. | Closed: the shell seeks `RootScene` to the arriving blade's rest frame (43 / 21 / 68 / 118 / 168) or plays a boot range, exactly as `XuiTabScene` and the boot dispatcher do. `__dash.invisibleAtRest` still reports the authored state of the scene. |
| The "Sign Out" and Y legend captions | Both mean something only once a profile is signed in, and sign-in state was code, not scene data. The scene ships the signed-in caption because that is the common case. | With no profile — the state the footage is in, and the shell's default — the caption is cleared and the glyph drawn disabled, which is what the frames show. `?signedin` restores the authored text. Nothing is invented either way. |
| Text when `reference/fonts/xtt/*.xtt` is absent | The Convection face is not in the dashboard archive; it ships in the console's system flash. | The runtime loads `public/assets/6770/fonts/ConvectionUI.ttf` decoded from the console files by `tools/xtt2ttf.py`; only when those files are missing does text fall back to the browser's sans-serif, flagged in `__dash.placeholders`. |
| The Xbox logo boot sequence | It is not in the dashboard archive and it is not XUI. The whole 6770 asset set is 3,234 `.xus`, 295 PNG, 263 `.xur`, 16 `.xma`, 6 JPG, 5 `.scb` and one `.ox` — no video container of any kind, and the one `DashVideo` element in the corpus (`oobe/oobeVideo.xur`) has no `File`. The PE's `.XBMOVIE` section is 12 declared bytes of pointers into a Media Foundation property-key table for music metadata, not a movie. The chrome sphere, the ignition flare, the ring of light and the 5.08 s hold are rendered by the console's boot chain before `dash.xex` runs. | Nothing is shown for it. The dashboard starts where the console handed over: `dashmain`'s own `BootLive` range, frame 462, which IS in the data and which the shell plays on first load. `__dash.shell.booted` names the range. |
| Code-driven lists: one of the offline tree's twenty-six stays empty for a hardware reason, ten for a device or Live reason | Fifteen are filled from the executable, each with its VA in `dashboards/blades/codeLists.ts`: Console Settings (0x920143d0), Display (0x927bfff0), Language (0x92016d8c + 0x92016dc0), Locale (0x92016eb8), Time Zone (0x927bf680), Passcode Hint (0x92015320), Remote Control (0x921c8d08), the Family Timer's single "off" row (0x921cb5e0 / 0x921cb4b0, string 383 - the state of a console with no profile and no timer), the five clock spinners (`%0*d` over the ranges 0x921cc9d4 / 0x921cca10 / 0x921cce2c / 0x921cce5c / 0x921cd128, parked on the console clock, which is the host clock here), and the game and movie rating lists, chosen by the console's `XC_LOCALE` out of the 29 tables at 0x920163a0 through the 39-row locale table at 0x92016530 - United Kingdom [FRAME 6717/f0060] gets the PEGI + BBFC game table (system 4) and the BBFC movie table (system 2). The TV rating list stays empty because the UK row's TV system is 7 = none, on which `dashCRatingView`'s init returns before touching the list (0x921bd584). The HDTV mode list PREPENDS a row from the attached display's reported native mode (0x921c5688) - no display here. The rest are device or Live state: the storage devices (`memory/DeviceSelector#list_devices`), the media sources on the network (`dashcomm/MediaSourceSelection#listMediaSources`), the pictures, albums and songs on a device (`pictures/905`, `music/*`), the Arcade titles, categories and friends (`arcade/*`). | Every empty list says why in `__dash.shell.codeUnfilled`, one line per list, and draws NO template row (an empty `XuiList` is consumed by an empty `ListView`, so the skin's `control_ListItem` template is not painted as a blank row). No option text is invented anywhere. |
| The AV pack behind the Blades Display page's switch art (M3f, Judge E round 3 finding 4) | `XGetAVPack` - which cable is plugged in - is console state, and on this page it decides a VISUAL, not a caption. `consoles/dashSysCslSetDisplay.xur` authors a `SwitchImage` group (HDTV2TVSwitch.png and a "TV" label) SHOWN over the list column, and `dashVideoSettings::UpdateCurrentSetting` (0x921c6ef8) hides it FIRST - `SetShow(this+0x70, 0)` at 0x921c6f30-0x921c6f40 - and re-shows it (0x921c6ffc-0x921c7004) only on the branch where the resolution provider read `XGetAVPack == 0`, the same branch that pushes `dashCSettingsStrings[553]` into `labAVPackInfo` [CODE]. | The reference console is on an HD pack, and that is a READING of two frames, not an assumption: f0053's "1080p" is a line only the 4/6/8 branch of 0x921c6c40 can format (every other pack emits string 354 "TV"), and its value carries no PAL-50/60 line, which 0x921c6548 suppresses for exactly those packs. So the shell takes the non-zero branch and the art stays down - `dashboards/blades/displaySettings.ts` `REFERENCE_AV_PACK`, with that reasoning as its `source` string, reported in `__dash.shell.hardwareState` and gated in smoke-nav §9f. Which of 4, 6 or 8 it is, no frame and no code path separates, and nothing on screen depends on it. |
| The "Current Setting" values on Console Settings, every settings page's own "Current Setting" label, and every `<setting>` caption | Console state: what the settings pages READ (XConfig through `ExGetXConfigSetting`, the xam audio flags, the parental block at 0x92872c90) and WRITE on A. The console wrote each value into `Pane_txtCurrentSetting` (`DataAssociation` 4 in `metaScene_1line`) and into `labCurrentSettings` on the pages, over the 168 controls across the corpus whose authored `Text` is nothing but an angle-bracket token (`<setting>`, `<servicename>`, `<free space>`, `<MAC Addr>`) - placeholders the authoring tool left, which the console always overwrote, so the token itself was never on screen. | **Blades 6770:** the values are DERIVED from a console state (`dashboards/blades/settingsModel.ts`) by the same rules the executable's handlers use - each option page's init, press and Console Settings provider is decoded there with its VA - and that state starts where the 6717 stills put the reference console, every value cited to its frame: Display "1080p / Widescreen / Standard" [f0053], Audio "Dolby Digital / Sound Effects Enabled" [f0055], Themes "Xbox 360 (Default)" [f0056, the no-dash-user path of 0x921cabc0], Language "English" [f0057], Clock "GMT+00 London" and the 24-hour format [f0059], Locale "United Kingdom" [f0060], Startup "Xbox Dashboard" [f0061], Shutdown "Auto-Off Disabled / Background Downloads Enabled" [f0062], Screen Saver "Screen Saver Enabled" [f0063], Remote Control "All Channels" [f0064], and System Info this build's own "Dashboard: 2.0.6770.0". Two things start UNKNOWN and are listed in `__dash.shell.hardwareState`: the daylight-saving bit (the clock line does not show it) and the Family Settings block (no capture enters it). Unknown is the console's own failed-read path: the page keeps its `DefaultFocus` and its label stays blank until a selection is made. The clock's own value is the host clock (the reference RTC sat at its factory default, "22/11/2005 12:00" [f0059]). Every angle-bracket token is CLEARED on the page and on every sub-scene mounted into it afterwards (metapane scenes, banners), and the smoke suite gates on painted DOM text on every page it reaches; a value the build's strings spell with angle brackets ("<None>", string 427) is a value and is painted. **That sentence was FALSE until M3h and this is what it hid** (Judge E round 5): the clear was ANCHORED (`/^\s*<...>\s*$/`), so it only cleared a Text that was nothing but ONE token. The corpus has **211** controls whose authored Text carries a token; 192 are wholly one token and **19 carry a token among other words, digits or a second token** - and all 19 walked straight through, two of them on screen (`memory/DeviceSelector#labTotal` painted "<#> of <Total #>" under "No storage devices found.", `arcade/2504_TitleOptionsScene#lblRatingText` painted "<www.pegi.info: 3+ with mild>" and two "<Rating Information>"). The walk's own detector and the smoke gate carried the same anchor, which is why round 4 read "0 painted tokens on 447 screens". The rule is now a SEARCH (`AUTHORING_TOKEN`, `paintsAuthoringToken`) and it is safe to widen because every one of the 211 texts in the build that contains a "<" IS a token - there is no HTML body and no prose with an angle bracket in any of the 263 scenes. The console's writers never patch a token in place: each one replaces the whole caption (`SetText` 0x92158f40) or hides the control, so a caption carrying a token anywhere is a slot in its entirety. **All 19 are named in `TOKEN_SLOTS` (`dashboards/blades/consoleSettings.ts`) with the console rule that filled each**, and the shell appends that reason to its `hardwareState` line. The two reachable ones, traced to the instruction: `labTotal` is HIDDEN and never re-shown (`DeviceSelectorScene` binds it into the controls block at +0x10, 0x9225af38, and the block reset 0x9225ace8 - reached from the scene load at 0x9225b1d4 - runs `Show(labTotal, 0)` at 0x9225ad08-0x9225ad10; the memory pack's only two "n of N" writers, 0x9225fe78 and 0x92263ea0, belong to other scenes and hide their own label when the list does not scroll); `lblRatingText` is written to the EMPTY STRING (`0x9221cbe8` reads the title record at this+2196, writes `record + 7718` when it carries a rating, and on the no-rating arm 0x9221ccd0 hides the pane's own carrier at this+2184 (a field the ctor zeroes and no name bind fills, so it is not named here) and writes `SetText(lblRatingText, L"")` from the empty literal 0x92001cd4). Gated over all **50** reached pages by `smoke-nav` §11 with a global search, 0 painted tokens. **NXE 9199 is its own row below** (M4d): `<setting>` is painted on seven of its eight Console Settings sub-pages, which is open there (COVERAGE N2). |
| The banner in every `XuiBOTDOfflineContainer` | Online, the container held a banner served by Xbox LIVE. | NOT a placeholder any more, offline: the container names its own offline scene in `XuiBOTDOfflineContainer.DefaultBanner` and the shell loads it, so the Xbox LIVE blade shows `botd/defaultbanner0.xur` and the ad tile `defaultbanner1.xur` — the same panels the footage shows. The LIVE-served banner remains Live-dependent and is listed above. |
| ~~Which row the Xbox LIVE and Marketplace blades come up focused on~~ | CLOSED, and it was never a placeholder on Marketplace: `blademp/marketplaceSignedOut.xur`'s `ScriptScene` root DECLARES `DefaultFocus="scnBanner"`, and `marketplace.scb` has four `onpress` handlers and never touches focus. On Xbox LIVE `DashLiveSignedOut` (registered 0x9228f060, bound at 0x9228f478) fetches five children and makes no `SetFocus` call, so focus is the XUI runtime's own default: the head of the scene's authored chain, `fakeGamerCard`. `f0078` is an arrival frame (`f0077` Games, `f0079` Marketplace — one sideways sweep, no vertical input) and shows the Create Profile card wearing the focus gradient. | Nothing stands in. Both blades report a real `focusId`. |
| The number of profiles on the console, and whether a disc is in the drive | Device state, not scene data. `DashLiveSignedOut` picks its caption from the profile count (`dashStrings.xus[173]` "Create Profile" with none, `[174]` "Sign In" with one) and `DVDTrayScene` picks the tray caption from the drive state (the switch at 0x921b2054: 1→[196], 2→[195], 3→[204] "Opening", 4→[206] "Reading", fall-through →[203] "Open Tray"). | The state assumed is the one the footage is in and the shell's default: **no profile and no disc**, so `[173]` + `[52]` "No Profiles Found" on the gamercard and `[203]` "Open Tray" on the tray strip. Both are real strings from the build, chosen by the console's own rule; `?signedin` picks the other caption. |
| The xam message boxes: Initial Setup, Background Downloads, the PAL/HDTV "keep these settings?" and the Family Settings save/reset prompts | `XShowMessageBoxUI` is system software (`xam.xex`), reached through the wrappers at 0x92114a98 (Initial Setup: dashStrings [176] "Initial Setup", [179] "Do you want to run initial setup?", [177] "Yes, run setup", [178] "No, don't run setup"), 0x921a63f0 (Background Downloads' Enable: dashCSettingsStrings [42] / [41], dashStrings [246] "Yes" / [50] "No") and 0x92114898 (the display and Family Settings prompts). The box's chrome is not in any pack; only its strings are in the build. COVERAGE B4 guessed "the dialog scene is `dashcomm`'s msgbox, to be identified": it is not there. `dashcomm/` holds nine `.xur` and none is a message box, and the only alert-shaped scenes in the whole 6770 build are five pack-specific ones (`dvd/DvdAlert`, `music/1023_TroubleshootConnectionAlert`, `music/1024_DownloadInfoAlert`, `music/1031_SaveChangesAlert`, `network/2030_ConfirmAction`), each with its own authored body text rather than the title/body/two-button shape these four calls pass. The generic box is xam's, and xam is not in this archive. | Not drawn and not faked. A press that the code gates behind a box takes the code's own no-answer branch - Initial Setup opens nothing, Background Downloads' Enable writes nothing and pops - and the box is recorded with its four strings in `__dash.shell.dialogs`. The OOBE that a "Yes" would run (`oobe/oobeWelcome.xur` and its pack siblings are all in the dump) stays behind that box. |
| `Personalization.xur` - the Themes picker | Console Settings' Themes row is the one 0x920143d0 record with no scene: its alt handler (0x921cad40) opens `Personalization.xur` (string 0x9200179c), and that file is in no pack of this archive. An ARCHIVE gap, not a Live one; it sits beside the guide and the boot video. | The row's Current Setting is the build's own no-dash-user value "Xbox 360 (Default)" (string 126 [FRAME 6717/f0056]); A on the row is a recorded code path and opens nothing. |
| The storage device a title sits on, and the Arcade pages' two `btnX` legend discs (M3h, Judge E round 5 finding 3) | `arcade/2504_TitleOptionsScene` authors FIVE storage-device indicators - `HD`, `MUA`, `MUB`, `OD`, `BuiltInMU` - on three design points, with MUA/MUB stacked at (940.679, 95.802) and HD/BuiltInMU at (967.679, 95.802). They are ALTERNATIVES, and which one lights is device state: `Arcade::CTitleOptionsScene` at **0x9221c558** reads the selected title record (this+2196), leaves all five flags 0 when there is none, else switches on `0x922297b0(title)` - 1 `HD`, 2 `BuiltInMU`, 4 `OD`, 0x10000002 `MUA`, 0x20000002 `MUB` - and runs the five `Show(x, flag)` calls at **0x9221c5e8-0x9221c620**. | **Not a placeholder any more: the code's own answer is used.** There is no title offline, so all five are hidden (`CONTROLS_HIDDEN_OFFLINE` in `codeLists.ts`, every copy under the level) and the reason is in `__dash.shell.hardwareState`. Before M3h the page drew the MUA and MUB glyphs on top of each other with no memory unit attached. The related stacking on `arcade/2502_TwistSelectorScene` IS the console's and stays: `Arcade::CTwistSelectorScene` shows the scene's own `btnX` (this+2224) whenever the active tab's content is up (0x922243a4-0x922243b4, hidden at 0x92224478), while `Arcade::CTitleSelectorScene` owns Tab1's own `btnX` (this+2212) and only ever enables and captions it (0x9221e4e4, 0x9221e520) - it never hides it. Tab1 is at (142,196) and its `btnX` at local (4,442) = (146,638); the scene's own is authored at (145.806,638). Two identical disabled discs at one point is what the console draws. `smoke-nav` §11b gates that no OTHER pair on any of the 50 reached pages shares an authored box. |
| The storage devices, the media sources, the camera, and the profile a "Create Gamer Profile" would make | `memory/DeviceSelector` enumerates attached storage (0x9225aec8), `dashcomm/MediaSourceSelection` discovers media-sharing PCs on the network, `consoles/dashSysLiveVision` reads the camera state (0x92356ef8) and `oobe/oobeProfileCreation` creates a profile on the console. None of them is in the archive; none is Live either. | Each page is the console's own EMPTY state, from its code: no storage device (`txt_EmptyList` "No storage devices found." shown, `list_devices` empty and disclosed), no computer (`NoComputersScene` shown and the "Please wait" pair hidden, 0x921aac44), no camera (the three choosers disabled and `NoCameraTextField` shown, 0x921cda90, so the page arrives with no focus), and the OOBE wait page as the honest stop of the profile chain. |
| The console's serial number, console ID and firmware version on System Info (M3g, Judge E round 4 finding 2) | `consoles/dashSysCslSetPolicyInfo_System.xur` is authored as a copy of the factory-reset screen and still carries that screen's prose in its `XuiEdit edInfo`. The console never showed it: `dashSystemReset`'s init at 0x921c8568 binds `edInfo` (L"edInfo" at 0x92016944, resolved at 0x921c8580-0x921c85a0), formats a string over four console values and `SetText`s the result at 0x921c8794-0x921c879c. Which string is the IPTV-provider branch at 0x921c86f4 - `dashCSettingsStrings[546]` when `0x9226e7d8()` reports a provider (it adds a `%s GUID: %hs` line), `[545]` when it does not, which is the reference console (seven System rows, [FRAME hi f0051], the same predicate that hides `navIPTVSettings`). Of 545's four fields, three are hardware: the serial (`0x9273a9cc(0x14)` at 0x921c85d4, 12 chars), the console ID (`0x9273ab7c` at 0x921c85f4) and the whole `D:` diagnostic line, which 0x9273aa1c builds from 0x92016908 `"%s - K:%d.%d.%d.%d (BK:%d.%d.%d.%d) X:%04X-%04X-%04X-%04X"` over the version records reached through 0x92000b08 / 0x92000ccc - XEX import thunks the loader patches, so this archive holds no firmware version at all. | The page paints string **545 itself**, with the three hardware fields left on the code's own empty-buffer path (a failed serial read stores exactly that, 0x921c85d8-0x921c85e8) and each one disclosed in `__dash.shell.hardwareState` with its read address. Everything the archive DOES settle is painted from the build: the product line, the copyright year **2008 from the code literal `li 0x7d8`** at 0x921c8730, and the warning paragraph. Nothing is invented and the factory-reset prose is gone; `smoke-nav` §10c gates the painted body and §10b fails if any reachable page paints prose the code replaces (`systemInfo.ts`, `CODE_WRITTEN_TEXT`). |
| Whether B does anything on the four network scenes that author an enabled "Back" with no `PressKey` (M3g, Judge E round 4 finding 4) | `network/2008_ActivateConfiguration`, `2030_ConfirmAction`, `2032_connecNow` and `2040_Ad-HocWirelessSecurity` each author a `XuiBackButton legend_b`, `Enabled=true`, `Text="Back"`, and set no `PressKey`. `XuiButton.PressKey` defaults to **22592 = 0x5840 = `VK_PAD_A_OR_START`** [xui.h 551, reference/xzp-tool/XuiElements.xml:69] and `XuiBackButton` derives from `XuiButton` without adding one, so an unset PressKey binds A - the same reason no `legend_a` in the build authors a PressKey while every `legend_x` / `legend_y` authors 22530 / 22531. Those four pages therefore put two controls on A and none on B: an authoring slip. XUI exports `XuiControlIsBackButton`, so its input router may reach a back button by class instead, but that router lives in `xam.xex` and is not in this archive. | Nothing is guessed. B on a page with no 0x5841 carrier pops the scene and presses no button, which is what the shell already does everywhere (`ShellReport.backCarrier` is null and no `btn_Back` plays); none of the four scenes is reachable offline, so it changes no pixel here. If the router's rule is ever sourced, it goes through the same pipeline. |
| The guide (the panel the Guide button slides in) | It is not in the dashboard archive: no scene in the 29 packs or `shrdres` names it, `dash.xex` contains no guide strings, and the footage shows a single silver panel drawn by the console's system software (`xam.xex`/`xshell`, in system flash, outside this archive). | Not implemented and not faked. The Guide button is a no-op that logs `__dash.placeholders`. If the system software's scenes are ever sourced, they get the same pipeline. |

## NXE 9199

The same rule applies: everything on screen traces to a `manifest.json` entry
whose hash came from the archive, and the exceptions are here. The fidelity
judge reviews these the same way.

| What | Why it cannot be original | Stand-in |
|---|---|---|
| The Aura background (`controlpack://aurascene.xur` as an `ImagePath`) | Not missing and not Live-served: `controlp/AuraScene.xur` IS in the archive and mounts with zero unknown classes. What is missing is the MECHANISM - XUI renders a `.xur` into a TEXTURE and `dashmain/DashBkgnd.xur` uses that texture as an image, and this runtime has no offscreen render target. The scene also carries **35 of the build's 36 `XuiShader` elements**, including the `themeripple.uxfx` cross-fade. | `dashmain/DashBkgnd.xur` is mounted behind the shell and `AuraScene` is mounted as a LIVE DOM SUBTREE where the `Aura` image would be - the same approximation `PanelScene`'s `XuiTextureSurface` already is, marked `data-xui-placeholder="scene-texture"` and counted in `__dash.approximatedClasses`. A render target flattens its contents before anything samples them and a subtree composites in place; no frame in the material separates the two. `__dash.nxe.aura` names every approximation on every load. **History, because the shape of the error mattered**: through M4b the scene's thirty-three `white.png` quads were drawn as pictures, which put thirty opaque 820x820 white plates on the floor and one 538x192 slab in the bottom-right corner. They are not pictures - each is the DRAW SURFACE of the `XuiShader` that immediately precedes it, and `xenonripple.uxfx` has no texture sampler at all - so they draw nothing now (`isShaderSurface`, swept: 33 elements in 9199, none in 6770). Binned by frame luma the residual went from +157/+177 at the dark end and -39/-72/-87 at the light end to +16/+17 and -11/-15. **What is left and is NOT tuned away**: the floor under the front panel reads 115/116/95/94 at rows 572/590/610/630 against the frame's 184/192/188/173, and the ablation says it is `SolidBack`'s own authored stops (rgb 90,90,90 -> 60,70,80 -> alpha 0) rather than a missing layer. The table is in the runtime README, and `smoke-nxe.mjs` holds each floor bin to that residual so it cannot grow unnoticed. A grey wedge also remains at x<96, y 400-560 (the `Color` figure's rotated boundary), 40 luma dark at (600,500) [Judge F round 3]. |
| Themes | `AuraControl.BackgroundImage` and `BannerImage` are unset in every one of the thirteen scenes that carry an `AuraControl` [SCENE]: a theme is downloaded content. `controlp/defaultbackground.jpg` is the built-in fallback and is in the archive. | The `AuraControl` renders as a recorded container (`__dash.approximatedClasses`) and no theme is applied. `ThemeImageIndex` is read and reported, not acted on. |
| A `XuiShader`'s draw surface | 33 `XuiImage`s in `AuraScene.xur` are a plain `white.png` sitting immediately after a same-numbered `XuiShader` inside `Front/Rings_Constant` (30) and `Front/Rings_Pulse` (3). They are not pictures: the group's thirty timelines animate the SHADERS' `EffectParams` and never the images, and `xenonripple.uxfx` carries no texture sampler in its constant table - it is procedural. The quad is the surface the ripple is drawn onto. | They draw NOTHING and are marked `data-xui-placeholder="shader-surface:<file>"` and counted in `__dash.approximatedClasses`, the same treatment `common://CornerMask.png` gets. What the console draws there - the ripple rings themselves, at `EffectParams4.x <= 0.1` - is a D3D9 pixel shader and is not reproduced. The rule is a corpus sweep, not a guess: 33 hits in 9199, all in this one scene, and 0 in 6770. |
| `reflection.uxfx` and every other `.uxfx` shader | A `.uxfx` is a UXFX header plus a compiled `ps_2_0` PC blob and a `ps_3_0` Xbox blob, with the uniforms readable straight out of the `CTAB` (`reflection.uxfx`: `ColorFactor`, `EffectParams2`, `Texture1`). Running D3D9 bytecode in the DOM is not possible, and this runtime has no WebGL. | The panel reflection is a live DOM copy of the surface's subtree with a CSS alpha ramp - opaque at the floor line, gone by 55 % of the mirror's height - and a flat dim. It is marked `data-xui-placeholder="shader"` in the DOM and the element is counted in `__dash.approximatedClasses`. `EffectParams2 = (-1.5, 0.2, 0)` is the shader's own `float3` constant and is recorded, NOT interpreted. `themeripple.uxfx` is mounted with its `Theme` group and animates NOTHING, because both of its `XuiImagePresenter`s (`Image_Current`, `Image_Next`) are theme data this archive does not carry; the group is marked `data-xui-placeholder="shader:themeripple.uxfx"` and its authored `EffectParams1 = (1152, 672, 0)` / `EffectParams2 = (0.05, 0, -1)` are recorded, NOT interpreted. |
| `XuiTextureSurface` (7 scenes) | A real render target flattens its contents before anything reads it back. | The subtree itself, and a second live copy for the mirror. Where two children of a slot overlap with partial alpha the console composites once and we composite twice; no frame in the material separates them. Recorded per scene. |
| The home page's "(A) Select" legend caption | The home page hosts no `LegacyControl`, so there is no parked `legend_a` for the shell to hoist - and every capture shows the caption anyway [FRAME Yrt f0483, Kpa f0048, Yv5]. | NOT invented: it is the focused slot's own `<onclick><helptext>` from the channel XML - every My Xbox slot writes `%EvResStr(IDS_SELECTSLOT)%` - resolved through `homepage/strings.xus`. `IDS_SELECTSLOT` is one of the three `IDS_` names the `.rdata` pair does not resolve, and the fallback cannot go wrong here: both "Select" entries in that table ([18] and [22]) are the same word. `__dash.nxe.legend` names the slot and the token it came from. |
| `XuiAvatar` (5 scenes) | The model, the textures, the animations and the user's own avatar data are none of them in the archive - they come from `xam`/Live. The class's thirteen properties are camera and animation controls [CODE 0x92485454]. **The SIGNED-OUT case is different and M4b had it wrong**: it is not a model. `dash.xex` loads `AvatarSilhouette.png` (436x730) and `AvatarShadow.png` (128x128) out of `dashcommon.xzp` at start-up and caches both [CODE 0x921421ec / 0x92142230], and both files ARE in this dump. | With no profile the shell draws `dashcomm/AvatarSilhouette.png` - the build's own artwork, nothing invented - `contain`-fitted to the `XuiAvatar`'s authored 776x776 box. The avatar viewport's CAMERA is not in the archive, so the two centring offsets are MEASURED off [FRAME Kpa f0048] ("Sign In" over "3 Profiles Found") and named as measured in `__dash.nxe.avatars`: figure top 270 against the frame's 268, height 269 against 283, head band 762..821 against 760..820. The 4.9 % height shortfall is the avatar's deliberate `z = -50` - it stands in front of the panel on the console and this runtime renders the slot flat, which is worth 2.7 % - and it is stated rather than scaled away. `AvatarShadow.png` is loaded by the same code and is NOT drawn: nothing says where the console puts it and the floor under the figure carries the panel's reflection. A signed-IN avatar still draws nothing. |
| Everything Xbox LIVE served | `homepage/emb_homepage.xml` carries only the OFFLINE channel set. The channels the online footage shows above My Xbox - Spotlight, Game / Video / Music Marketplace, Inside Xbox, My Community - are served by Live and are in no file in this archive. | The shell composes the seven channels the XML declares and that pass offline, and `__dash.nxe.channels` lists all eight with the predicate that kept or dropped each. Nothing is added to the list. |
| The gamer card's contents, "Latest Games", the gamerscore | Live and profile state. `slots/GamerCardSlotScene.xur` draws its own labels and leaves the data slots empty, which is what the scene says. | The slot renders its authored chrome with no data. `GamercardContainer` in `homepage.xur` is `Show=false` as authored and stays that way with no profile. |
| A Moby slot's background picture and its big icon | The eleven slot scenes declare NO `ImagePath` and NO `Text`: the console's slot class sets both in code. **The pictures are all in the archive** (`slots/BlankGreen.jpg`, `Games.jpg`, `Music.jpg`, `Photos.jpg`, `Videos.jpg`, `TrayBackground.JPG` and eight icons) and `dash.xex` names every one as a wide literal in one `.rdata` cluster, 0x9202a064-0x9202a2bc, laid out in the emission order of the slot classes at 0x92029c60. What is NOT recovered is the store that binds each literal to its class. | `dashboards/nxe/slotArt.ts` pairs them by that adjacency and by the file names, and **every row is marked `inferred` in `__dash.nxe.slotArt`** with which half is the reading. No file outside the archive is used and a row whose file the manifest does not carry is dropped and reported. The five slots with an icon and no background of their own take `BlankGreen.jpg`, the only background left in the cluster. Captions are NOT inferred: they are each slot's own `<description>` out of the channel XML, resolved through `homepage/strings.xus`. |
| Whether a disc is in the drive | Device state, as in Blades. | The state assumed is the footage's and the shell's default - no disc - so the tray slot's caption is `dashcomm/dashStrings.xus[129]` "Open Tray", the same table that carries [122] "Close Tray", [123] "Closing", [130] "Opening" and [132] "Reading". A real string from the build, chosen by the console's own rule. |
| Nine-grid corner masks (`common://CornerMask.png` at `ColorWriteFlags 8`) | The element writes the ALPHA channel only and no colour: it rounds a panel's corners by clearing alpha outside them. The DOM has no destination-alpha write. | It draws NOTHING, and is counted in `__dash.approximatedClasses`. The visible consequence is square corners on every Moby slot. Drawing it as a picture would cover the whole slot, which is what made the first render black. |
| `SolutionsSlotScene.xur` | `xbox360channel.xml` declares the Solutions slot, the Epix id table at 0x920289d8 carries `EsSolutions`, and the wide literal `SolutionsSlotScene.xur` is at 0x92028d7c - but no such file exists in the `slots` pack [SCENE]. | Nothing stands in. Offline the slot is dropped by its own `!EcoLiveTier(None)` condition before the file is ever wanted, and if it is reached it is reported in `__dash.nxe.unresolvedEpix` with the reason. |
| Images the 9199 packs do not carry | Four paths the 9199 scenes name and the 9199 dump does not have: `common://updis.png`, `sharedres://GScore.png` (the pack carries `GScore_white.png`), `sharedres://ico_96x_MSPoints.png`, `sharedres://splash_360.png`. Each was checked against the manifest, which is the extraction's own record of the pack bytes. | Nothing is substituted; a same-named file from another build or a recolour of `GScore_white.png` would be invented artwork. Each is one entry in `tests/smoke/allowlist.json` with its reason. |
| Five visuals no 9199 skin defines | `prgbr_VideoPreload`, `box_green`, `box_orange`, `tab_active_glow_2`, `tab_active_glow_3`. Each is named by exactly one scene and defined nowhere in the build - the same fact as Blades' `txt_Starting`. | Recorded in `__dash.unresolvedVisuals`, never substituted. |
| The `…Ex` transition pair | Not an asset: `LegacyFrom` / `LegacyTo` / `LegacyFromEx` / `LegacyToEx` are all four in the 9199 skin and all four play. Which pair covers a page-over-page swap is what the archive does not say. | **MEASURED (M4d), and it is the PLAIN pair.** System Settings -> Console Settings on the 29.97 fps cut of the primary capture [FRAME Kpa f05630-05639, 187.67 s] is ten frames: the page region's luma runs 64 -> 75 -> 85 -> 93 -> 98 -> 99 and back 96 -> 88 -> 77 -> 67, i.e. `LegacyFrom`'s fifteen ticks and `LegacyTo`'s five-tick hold plus fifteen-tick ramp started together (twenty ticks), with the two pages crossing at half strength - the `…Ex` pair would take sixty. `navigation.ts` takes the plain pair everywhere. **M4b-M4c's "the ~1.0 s of change at Kpa 190.06-191.22 s rules the plain pair out" is withdrawn: that window is the Console Settings LIST being walked (f0378-f0381 are Display, Clock, Auto-Play), not a page swap.** Where the console uses the `…Ex` pair is not observed in the four captures; the pair is kept as the skin's data and chosen nowhere. `tests/smoke/smoke-nxe.mjs` measures the swap against the frames. |
| The UNIT of the thirty strip constants, and the shape of a fold | Not a missing asset: `controlp/Variables.xur` has all thirty and the ten `.xma` cues are extracted. What the file does NOT say is what `Acceleration 40` is 40 OF. | The unit is INFERRED as index units per second - the only reading that is not absurd, and the one under which the channel axis closes at exactly 0.300 s. **The shape of a fold is NOT inferred any more (M4d)**: it is the executable's per-panel cascade at `.text` 0x9248d6dc-0x9248d988 (progress 1 = open; back to front at `FoldSpeed x (visible+1) / 7` gated on `FoldNextRange`, front to back at `UnfoldSpeed` eased to `UnfoldMinSpeed` gated on `UnfoldNextRange`; offset `q x spacing`, opacity `min(1, 4q)`), plus the front slot's own rotation `TransitionPanel x pi/2` about a hinge 128 units to its left (0x9248d94c, 0x92488480), driven by the `From`/`BackTo` ranges of the same `Variables.xur` (dashboards/nxe/physics.ts, transitions.ts). What remains measured: one panel step 0.3416 s modelled against 0.367 s (8498) and 0.383 s (9199) - a residual of 1.5 and 2.5 frames of twenty-two - and the velocity SHAPE (model peak 0.45 of the move, console about 0.33). **The M4c claim that a channel change is "move, then fold, then unfold" (Judge F round 2 N3, measured at Yrt 238.48 s) is RETRACTED: that event was the Guide closing over the profile page.** A channel change is a MEASURED fade in place (`CHANNEL_SWAP`: out 6 ticks, beat 4, each new panel in over 12, front to back) [FRAME Yrt f07272-07283, Kpa f00735-00747], and the file's fold constants do not produce it: at `FoldSpeed 30` the fade would be two ticks and at `UnfoldSpeed 10` six. Nothing is tuned; the swap is quoted as the frames' numbers and gated against the frames. |
| ~~`snd_transitioninto.xma` / `snd_transitionfrom.xma`~~ | CLOSED (M4d), and they were never inferred: `controlp/Variables.xur`'s `SceneTransitions` group carries a `XuiSoundXAudio` called `TransitionSound` whose `File` track writes `snd_transitioninto.xma` on frame 29 of `To` and frame 39 of `BackTo`, and `snd_transitionfrom.xma` on frame 9 of `From` and frame 24 of `BackFrom` [SCENE]. The eight `Sound*` cues are the same shape: eight groups named for the code table's entries, each with a `Sound`..`SoundEnd` range that writes the `.xma` on its first frame. | The two fire from the range the shell plays (tagged `timeline` in `__dash.nxe.cues`); the eight are played by the glue on the same tick the range would fire them. |
| Which `.xur` an `EcNavTo*` command opens | ~~"materialised in code, not in a pointer array ... ONE row is bound, and it is marked INFERRED"~~ - M4e read the dispatcher: the 35 command names at `.rdata` 0x920288a0 index a u16 JUMP TABLE at 0x92028ad0 from the base 0x922d312c, and the cases that open a scene call one helper with a pack and a file literal [CODE]: `EcNavToSettings` (4) -> `consolesettings.xzp` / `SystemScene.xur`, `EcNavToGamesLibrary` (3) -> `arcade.xzp` / `ArcadeFilterScene.xur`, `EcNavToXboxBasics` (7) / `EcNavToWhatsNew` (8) -> `FirstRun.xzp` / the two root scenes, `EcNavToLiveUpsell` (0x16) -> `homepage.xzp` / `LiveUpsellRootScene.xur`, `EcNavToStorageUpsell` (6) -> `StorageUpsellScene.xur` (not in the archive), `EcNavToSolutions` (0x21) -> `solutions.xml` (Live). What the archive cannot follow: `EcNavToVideoLibrary` (5), `EcNavToMusicLibrary` (0x17), `EcNavToPictureLibrary` (0x18) and `EcNavToMediaCenter` (0x10) branch to functions (`bl 0x92242118` / `0x9222d9a0` / `0x922227f8` / `0x92306510`) that build their page from the videos, music and pictures on the attached storage and the PCs on the network - device state - so their scenes (`videos/FiltersRoot`, `music/1003_IndividualDevice`, `pictures/905_IndividualDeviceMain`, `dashcomm/742_SelectNetworkDevice`) are in the dump and the list that picks among them is not. | The five bound commands open their scene: Settings is the walked System Settings page, the four roots are strips (the Rome CHANNEL row below). The four library commands are REFUSED and listed in `__dash.nxe.unboundCommands` with the case address and the device state they need; a refused press is silent. Nothing is pointed anywhere the bytes do not point. |
| ~~The channel queue's brightness ramp, and its size ramp~~ | CLOSED, and neither was a placeholder: both are a ten-row, three-float table the executable builds on the stack in the queue's layout routine (`stfs` block at `.text` 0x9248b624-0x9248b680, called at 0x9248ca20). Each row is `(dy, scale, opacity)`; element *i* of the binder's `Next6..Prev1` array rests on row *i+1* and lerps towards row *i* or *i+2* by the signed channel progress [CODE 0x9248b8d0-0x9248b980 for the array, 0x9248b988 for the base y]. | Nothing stands in. The rows' `Position`, `Scale` and `Opacity` are the table's; `Queue\Current`'s base y is read from the scene. The frame CHECKS it and nothing was fitted to the frame: cap heights of 33 / 25 / 18 / 15 / 14 design px up the stack [FRAME Kpa f0048] against the table's predicted 33.0 / 24.8 / 18.2 / 14.9 / 13.2. M4b's "measured brightness ramp" (1 / 0.75 / 0.55 / …) turns out to be the SIZE ramp read as brightness - the opacities are 1 / 0.5 / 0.35 / 0.2 / 0.1 / 0 / 0 - and M4b's 36 px group slide is gone, because the code moves every row separately. |
| `Marker2` in `MobyChannelScene.xur` | The scene authors TWO 16x64 marker groups at the current row's own y, both `Opacity 0` with `Scale (-1,1,1)`, and the code lays them out. | `Marker1` is raised, because one bullet is what the frames show: on [FRAME Kpa f0048] a small square sits at design x 82..92, y 195..205, which is exactly `Marker1`'s mirrored box. `Marker2` is left as authored rather than drawn on top of the first, and is recorded here. **The rule the captures show (Judge G round 3): the bullet is drawn while the console is SIGNED OUT and not while a profile is signed in.** Present on every signed-out frame - [FRAME Kpa f0048], [FRAME Yv5 f0042] - and absent on every signed-in one - [FRAME Yrt f0483], [FRAME Yrt f0268], [FRAME Yrt f0484], [FRAME Kpa f05585]. The shell is signed out and draws it; a signed-in state would not, and which profile property the code reads for that was not traced. |
| The gamer-card slot with no profile | `slots/GamerCardSlotScene.xur` authors BOTH states and expects the console to pick one: `UserGroup` (gamertag, gamerscore, "Latest Games") and `SignedOutGroup` (`Signin` on DataAssociation 5, `ProfilesAvailable` on 6). Its `<onclick>` is `KeyDown` A, and the slot class's handler opens `signin/SigninScene.xur` with no profile [CODE: `CSigninScene` registered at 0x922e2f34, the literal materialised at 0x922df3b8; FRAME Kpa 48-56 s, Yrt f0264-f0268]. | The console's own rule, as in Blades: with no profile the shell shows `SignedOutGroup` and fills it from the build's own strings - `dashcomm/dashStrings.xus[91]` "Sign In" and `[83]` "No Profiles Found". **9199 has no "Create Profile" string in any positional table**, so the Blades caption pair does not apply here; the Sign In page's panels carry their captions AUTHORED (`signin/CreateProfilePanelScene.xur`'s XuiText "Create Profile", `RecoverProfilePanelScene.xur`'s "Recover Gamertag"). `[98]` is a second identical "Sign In", so which index the code reads cannot change the pixel. **A opens the Sign In page (M4e)**: a MobyRootScene strip of `ProfilePanelScene` x local profiles (none offline), `CreateProfilePanelScene`, `RecoverProfilePanelScene` in the code's order [CODE 0x922e409c-0x922e415c], under ONE queue row reading "Sign In" (`dashStrings[91]`; the footage draws the word where `Queue\Current` sits and no other row) and a "1 of 2" counter. Its legend reads "(A) Select (B) Back" on every capture while the root's own parked `legend_a` says "Continue" with Show=false, so the two captions are SUPPLIED from `dashStrings[97]` "Select" and `[2]` "Back" - the build's words, the indices INFERRED and named in `__dash.nxe.legend`. A on a panel starts xam's profile creator / recovery flow (system software) and is recorded in `__dash.nxe.codePaths`; with a profile signed in the slot opens the profile page (profile state) and is refused with that reason. ~~"The Sign In page behind A is NOT BUILT (COVERAGE N3)"~~ - it is built and mounted by input (M4e); the earlier "the slot scene has no handler in this archive" refusal was wrong and is gone. |
| The guide | As in Blades: no `.xur` in any of the 29 packs or `shrdres` matches guide/hud/mini/xshell, and the footage shows it drawn by the console's system software. The 9199 capture shows it as a centred sheet reading "The Xbox Guide is not currently available" on that Proto console [FRAME `Yrt f0409`]; the 8955 capture shows the real one sliding in from the left. | Not implemented and not faked, exactly as in Blades. |
| The Rome CHANNEL (`ColumnLayer` and its counter) | ~~"the offline archive declares none"~~ - it declares four, and the Moby shell a fifth (M4e). A pushed ROOT scene is an empty 1280x720 host the code fills with a strip: `firstrun/WhatsNewRootScene.xur` (8 panels: the 8 x (flag, id, scene) table at `.rdata` 0x9202b63c [CODE]; `WhatsNewAvatarsScene.xur` sits only in a 4-row table at 0x9202b608 whose consumer was not traced and is not mounted), `firstrun/XboxBasicsRootScene.xur` (8 panels, the literals materialised at `.text` 0x922ed03c-0x922ed0e4 [CODE]; that the emission order is the strip order is INFERRED), `homepage/LiveUpsellRootScene.xur` (5 panels at 0x922d9908-0x922d9970, the channel order [CODE]; that the strip starts on the first whichever upsell slot was pressed is INFERRED), `arcade/ArcadeFilterScene.xur` (Recent Games, Collections: "2 of 2" [FRAME Yrt f0396], "6 of 53" [FRAME Kpa f0300]), and `signin/SigninScene.xur`, a MobyRootScene with one queue row. Each strip is `PanelScene` rigs on the ROME (or Moby) constants of `controlp/Variables.xur` - spacing 480, `RomeFrontPosition` (96,602), `RomeVisiblePanelDistance` 1850, the `RomeInput*` integrator - with `RomeOverlayScene`'s `Description` as the "%d of %d" counter at (96,605), and it plays the same `SceneTransitions` group's `To` on arrival and `BackFrom` on B. | INFERRED and said so in `__dash.nxe.legacy.strip.inferred`: that a pushed root plays `To`/`BackFrom` (XuiScene's ordinary pairing) and that the panels behind the front one emerge on `To`'s frame 70, where `TransitionPanel` returns to 0 (the rule the home page's B uses). MEASURED: the front Rome panel's four edges against [FRAME Yrt f0396] and the "2 of 2" counter's ink band; the second panel's left edge and top at spacing 480 against [FRAME Kpa f0300]; the Sign In strip's front and second panel, its "Sign In" row and its counter against [FRAME Yrt f0268] (tests/smoke/smoke-nxe.mjs). A Rome panel's own list (`CollectionFilterPanel.lstCollections`, filled from the title database; `RecentGamesFilterPanel.lstRecentGames`) is left EMPTY and reported in `__dash.nxe.codeUnfilled` with the six strings the capture shows named, not painted. **What the console does with an empty one is now DRAWN, not just described (M4f, Judge G round 3 F3):** `RecentGamesFilterPanel`'s refresh (`0x92271da0`) enumerates the title database into at most ten rows, and with none it raises the panel's own `labEmpty` ("You don't have any games in your library.", authored `Show=false`; `SetShow` true at `0x92271f48` -> `0x92271fcc-0x92271fd0`, false at `0x92271fc8` when there are rows) and disables `legend_a` and `legend_y` (`0x92271f04` / `0x92271f10`) [CODE]. No title is installed here, so that empty state is the console's own; it is applied from `CODE_VISIBILITY_9199` and reported in `__dash.nxe.legacy.hidden`. `CollectionFilterPanel` authors no such label and stays empty and disclosed. |
| The `SceneTransitions` group and the fold choreography | ~~"no scene in the build declares an object called SceneTransitions"~~ - `controlp/Variables.xur` declares a `XuiGroup` with exactly that id, holding the four `Transition*` variables and `TransitionSound`, with nine named frames (`To` 1-75, `From` 76-150, `BackTo` 151-225, `BackFrom` 226-300) and five timelines that animate the variables [SCENE, M4d]. The code reads them every frame through the same block the thirty constants are fetched into (`+0x08` scene opacity, `+0x10` the queue's fold at 0x9248ca28, `+0x14` the front slot's rotation at 0x9248d95c). | The group is mounted hidden and its `From` range played on A and `BackTo` on B; the queue rows, the counter, the front slot and the strip layer read the values back (transitions.ts). INFERRED: that the home page plays `From`/`BackTo` (the ordinary XuiScene pairing), that the page comes in at the frame `TransitionScene` starts to drop (frame 120), and that the panels behind the front slot emerge at frame 200. MEASURED and open: with `From` on the press the front slot's rotation lands where the footage has it (ramp at +29 ticks; [FRAME Kpa f05590]) and the queue rows fade earlier than the footage's [FRAME Kpa f05585] by 2-4 30 fps frames; the legend's `Hide` leaves ~0.3 s ahead; on B the front slot returns ~0.13 s ahead of [FRAME Yrt f07188]. `TransitionSubElements` is read by 0x9248ad48 and is bound to nothing here. |
| The "Current Setting" values on 9199's Console Settings | Console state, as in Blades: `Pane_txtCurrentSetting` is DataAssociation 4 of `metaScene_1line` and the eight descriptions start with three to six CRLFs to leave room for it [SCENE]. | Six rows carry the value the reference console showed, marked **hardware-state** and cited: Display "1920 x 1080 / Widescreen / DVI" [FRAME Kpa f0377, f0378, f0389], Audio "Dolby Digital / Sound Effects Enabled" [f0379, f0386], Language and Locale "English / Canada" [f0385], Startup and Shutdown "Start Xbox Dashboard / Show Welcome Channel / Auto-Off Enabled / Background Downloads Disa..." [f0384; the console's own ellipsis], Auto-Play "Auto-Play Enabled" [f0381, f0382], Remote Control "Both Remotes" [f0383]. Clock shows the capture's own minute ("05/27/2025 07:04", f0380) and is left blank; System Info is not focused in any capture and is left blank. Each read is listed in `__dash.nxe.hardwareState`. The DESCRIPTIONS are not hardware state: they are `dashCSettingsStrings.xus` [325], [323], [326], [324], [329], [327], [328], [330], the code table's second index. |
| A slot's second line: "565 GB free" | `mobyslot`'s `XuiTextPresenter2` (392x31 at (19,282), PointSize 16, Opacity 0.65) draws DataAssociation 1 [SCENE]. The Settings slot on the reference console shows "565 GB free" there [FRAME Kpa f05578-05590], the free space on its storage - device state. The same capture shows a "System Settings" caption at (96,~70), where `LTitle` sits, while the slot rests: the slot's `Label_Head` hoisted into the legend's title group by the same rule a page's is. | Media Center's `<description2>` (`homepage/strings.xus[13]` "TV and media from your PC") is written to association 1 and painted [FRAME Kpa f05545]; the Settings slot has no `<description2>` in the XML and its free-space line is not drawn - nothing says the number. The title observation is recorded here and not reproduced: the slot scenes carry no `Label_Head` label for the shell to hoist. |
| Which row the channel queue wraps to | The rows are laid out as if an Up on the last channel is not refused: `Next_n` is the channel n places after the current one, wrapping, and with N channels at most N - 1 rows above the current are filled [FRAME Kpa f0048 (seven), Yv5 f0042 (two: one name above, nothing else)]. | The cursor wraps and the rows are filled by `queueRowChannel`; that the wrap is what the console does on the key is INFERRED from the rows and stated in `__dash.nxe.physics`. |
| The channel change's second, quieter sound | The footage carries ONE audible onset per channel change, matching `snd_channelup/down`'s spectrum at 0.97 [AUDIO Yrt 242.500 s; Kpa 24.770, 25.675, 26.260, 26.510 s], and on Yrt a second onset 26 dB below it 0.34 s later whose spectrum matches `snd_panelfold`/`snd_panelunfold` at 0.99. The cue's mix level is XACT state this archive does not carry. | No fold or unfold cue is played on a channel change; the channel cue alone. On A the fold cue and on B the unfold cue are played at the cascade's start, and the capture's quieter onset 0.23 s after the select [AUDIO Kpa 186.100 s] is consistent with `snd_panelfold`'s 0.54 s length and inconclusive by spectrum (0.84 against 0.90 for `snd_transitioninto`). |
| The boot animation | No video container of any type exists in the 4,343 files of the 9199 dump. | Nothing is shown for it. Unlike Blades there is not even a `BootLive` range to start from: the NXE shell simply composes. |
| Console state behind the hosted pages' A (M4e) | Every option page under Console Settings (`btnOn`/`btnOff`, `btnNormal`/`btnWide`, `listOptions`, `lstLanguages`, `lstCountries`, `lstTimezone`, `listChannels` ...) writes the chosen value through XConfig on A and rewrites the parent's Current Setting; the value is device state this build has no store for (the Blades B1 model, not built here). | The row presses (its own `Press` range, `btn_Select.xma` from the skin) and nothing is written; each such press is recorded in `__dash.nxe.codePaths` with the reason. The `<setting>` line on every sub-page is CLEARED (it is the value the console would write) and listed in `__dash.nxe.hardwareState`; the six cited values on the parent Console Settings page stay as the frames show them. Nothing invented. |
| The AV pack behind the Display pages' switch art (M4f, Judge G round 3 F1) | `XGetAVPack` - which cable is plugged in - is console state, and it decides a VISUAL, not a caption. `consoles/dashSysCslSetDisplay.xur` and `dashSysCslSetDisplayHiDef.xur` both author a `SwitchImage` group (HDTV2TVSwitch.png + a "TV" label) SHOWN on the scene root, over the list column, and the console never draws it there on an HD console: `dashVideoSettings::UpdateCurrentSetting` (`0x92219790`) hides it FIRST (`SetShow(this+0x68, 0)` at `0x922197ac`) and re-shows it (`0x92219874`) only where the resolution provider `0x92219328` took its `XGetAVPack == 0` branch, which also writes `dashCSettingsStrings[571]` into `labAVPackInfo` (`li r4, 0x23b` at `0x92219430`); `dashVideoSettings_HD::OnInit` (`0x92219000`) is the same shape (`0x92219058` hide, `0x92219180` show) [CODE]. | The reference console is on an HD pack - its Display metapane reads "1920 x 1080 / Widescreen / DVI" [FRAME Kpa f0377] - so the shell takes the non-zero branch and the art is hidden, which is what the two pages now draw (0 dark pixels in the box the art would fill). `&avpack0` takes the other branch and shows the art AT THE FILE'S OWN PLACE: the code writes `Show` and only `Show` (the class reads `this+0x68` exactly twice in `0x92218c00-0x92219c50`, both of them the `SetShow` calls), so nothing is repositioned, and the group's `scnCurrentFormat` neighbour is an EMPTY nested scene that owns none of it. What the AV-pack-0 branch would ALSO do - the row builder at `0x92218cf8` rewriting each Display row's present / enabled field - is not applied and says so in `__dash.nxe.codePaths`. The state itself is in `__dash.nxe.hardwareState`. |
| Code-driven lists on 9199's pages | 9199's tables are its own (dashboards/nxe/codeLists9199.ts, every VA and record layout re-read from `extracted/9199/basefile.exe`): Display 0x927f0ae0 (7), Language 0x92018bfc + 0x92018c30 (13 labels, 5 region groups of stride 13; group 0 shown, the region rule 6770's [INFER]), Locale 0x92018d40 (37), Time Zone 0x927f0130 (65, not 6770's 75), Remote Control (`273 + (row != 0)` at 0x9221a674, count 2 at 0x9221a6c0), Pass code hints 0x9201a06c (5). What stays empty and why: the HDTV mode list (EDID), the five clock spinners (the console clock), the family-timer durations (a profile), the three rating lists (the locale pick), the storage-device and media-source lists (device state), Live Vision (no camera), the Rome panels' title lists (the title database). **Code-written CAPTIONS are the same rule (M4f, Judge G round 3 F4):** `network/2004_NetworkDetails.xur`'s `btn_IP` (`btn_4Line`) and `btn_DNS` (`btn_3Line`) author no `Text` at all, and drew as an empty bar and row. `C2004_NetworkDetails` writes their four lines from `network/Strings.xus` through the `C4LineBtn` setters `0x92290be0` / `c20` / `c60` / `ca0` (`li r3, 0x2d..0x30` at `0x922913a8-0x922913f4` for `btn_IP` = [45] "IP Settings", [46] "IP Address", [47] "Subnet Mask", [48] "Gateway"; `li r3, 0x29..0x2c` at `0x9229154c-0x92291598` for `btn_DNS` = [41] "DNS Settings", [42] "Primary DNS Server", [43] "Secondary DNS Server", [44] the empty string) [CODE]. Those LABELS are painted, from the build's own table. The right-hand VALUES are the network configuration - "Automatic"/"Manual" and four `%d.%d.%d.%d` addresses - which this archive has no reading of, and there is no "no network" caption for them in the table, so they are left blank and disclosed. | Filled lists are named in `__dash.nxe.legacy.codeFilled` with their VA; empty ones in `__dash.nxe.codeUnfilled` with the reason, page by page, and the smoke walker refuses a page that has neither rows nor a reason. A code-written caption is named in `codeFilled` with the table and the indices, and the values it leaves blank in `codeUnfilled`. |
| Disk in Tray, NXE Video, Hide Welcome Channel (M4e) | `TraySlotScene`'s `KeyDown` A ejects the tray (hardware); `EcPlayMigrationVideo` (0x0b) plays `homepage/VideoScene.xur`, whose `XuiVideo` names no file in the archive; `EcHideWelcomeChannel` (0x1a, case 0x922d34c0) raises a message box built from `dashStrings.xus` [26] "Welcome Channel", [25] "Do you want to remove this channel? ...", [174] "Yes", [81] "No" before it flips the setting - the box is xam's (system software) [CODE]. | All three are refused, silent, and listed in `__dash.nxe.unboundCommands` with exactly that reason; `&nowelcome` is the setting the box would flip. |
| Which parked legend carriers are hoisted | A page's `legend_a/b/x/y` are read by the LegendScene hoist; some are authored `Show=false` (`romechan.xur`'s and `LiveUpsellRootScene.xur`'s `legend_b` "Back", `SigninScene.xur`'s all four, `NetworkMain.xur`'s `legend_y` "Status"). | A `Show=false` carrier is NOT hoisted: the Rome channel page on the themed capture draws "(A) Select" and no B entry [FRAME Kpa f0450] with exactly that authoring. A whitespace caption (`742_SelectNetworkDevice.xur`'s `legend_x` = CRLF) is no caption. A label wearing `Label_Head` INSIDE its scene with Show=false (`arcade/ArcadeFilterScene.xur`'s `labRomeTitle` "Game Library") is not a title: the Game Library shows none [FRAME Yrt f0396, Kpa f0300]; only a PARKED header (y outside the plate) is hoisted. |
