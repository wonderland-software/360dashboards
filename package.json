{
  "name": "360dashboards",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "GPL-3.0-only",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts",
    "smoke": "node tests/run-all.mjs",
    "extract": "node --import tsx tools/extract.ts"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "puppeteer-core": "^25.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vite": "^8.1.5"
  }
}
