node_modules/
dist/
.DS_Store
*.local
.env
.env.*
.vercel

# Microsoft-owned inputs and the raw dumps stay local. The SERVED tree,
# public/assets/, is committed since 2026-09-03 (Tag's call) so the site
# builds from GitHub alone; see NOTICE. Recreate everything with: npm run extract
vendor/
extracted/
reference/
*.xex
*.xzp
*.xur
*.xma
*.wav
*.xus
*.xtt
!public/assets/**

# Rendered screenshots of the dashboard (smoke output) stay local like the assets they draw.
tests/smoke/out/

# Agent working notes, never part of the build.
scratchpad/
